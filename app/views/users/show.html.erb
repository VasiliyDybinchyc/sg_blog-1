<h2><%= @user.name %>, <%= @user.email %></h2>

<%= link_to @hash.empty? ? "My location" : "Change my location", new_map_or_edit, class: "btn btn-primary" %>

<div id="location-info">
  <%= render :file => 'geolocations/_show' if !@hash.empty? %>
</div>
