<h2><%= @user.name %>, <%= @user.email %></h2>

<%= link_to @geolocation.nil? ? "My location" : "Change my location",
  new_map_or_edit, class: "btn btn-primary" if current_user?(@user) %>

<div id="location-info">
  <%= render :file => 'geolocations/_show' if !@geolocation.nil? %>
</div>
