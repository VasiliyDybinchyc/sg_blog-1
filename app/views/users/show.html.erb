<%= @user.name %>, <%= @user.email %>

<%= link_to @hash.empty? ? "My location" : "My new location", new_map_or_edit, class: "btn btn-primary" %>

<%= render :file => 'geolocations/_show' if !@hash.empty? %>
